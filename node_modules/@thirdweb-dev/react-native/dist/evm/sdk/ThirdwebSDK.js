import { ThirdwebSDK as ThirdwebSDKCore, } from "@thirdweb-dev/react-core";
import { ThirdwebStorage } from "../../core/storage/storage";
export class ThirdwebSDK extends ThirdwebSDKCore {
    constructor(network, options = {}, storage) {
        super(network, options, storage ||
            new ThirdwebStorage({
                clientId: options.clientId,
                gatewayUrls: options.gatewayUrls,
            }));
    }
}
