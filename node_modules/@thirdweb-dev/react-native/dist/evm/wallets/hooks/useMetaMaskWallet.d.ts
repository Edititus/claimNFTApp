/**
 * const connect = useMetaMaskWallet();
 * connect();
 *
 * @returns connect function to connect to the MetaMask wallet
 */
export declare function useMetaMaskWallet(): (connectOptions?: {
    chainId?: number;
}) => Promise<import("../wallets/metamask-wallet").MetaMaskWallet>;
