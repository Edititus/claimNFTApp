/**
 * const connect = useRainbowWallet();
 * connect();
 *
 * @returns connect function to connect to the Rainbow wallet
 */
export declare function useRainbowWallet(): (connectOptions?: {
    chainId?: number;
}) => Promise<import("../wallets/rainbow-wallet").RainbowWallet>;
