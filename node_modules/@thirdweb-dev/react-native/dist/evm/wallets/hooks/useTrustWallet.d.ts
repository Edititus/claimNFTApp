/**
 * const connect = useTrustWallet();
 * connect();
 *
 * @returns connect function to connect to the Trust wallet
 */
export declare function useTrustWallet(): (connectOptions?: {
    chainId?: number;
}) => Promise<import("../wallets/trust-wallet").TrustWallet>;
