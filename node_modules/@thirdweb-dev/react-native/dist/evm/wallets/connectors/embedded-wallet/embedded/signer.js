import { getExistingUserEtherJsWallet } from "./helpers/wallet/retrieval";
export async function getEthersSigner(clientId) {
    const wallet = await getExistingUserEtherJsWallet(clientId);
    return wallet;
}
