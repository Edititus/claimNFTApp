import { Magic, MagicSDKAdditionalConfiguration } from "@magic-sdk/react-native-bare";
import { Chain, Connector } from "@thirdweb-dev/wallets";
import { Signer, providers } from "ethers";
import { MagicConnectorOptions } from "./types";
export declare class MagicConnector extends Connector<MagicConnectorOptions> {
    magicSDK: Magic;
    magicOptions: MagicConnectorOptions;
    provider: any | undefined;
    chains: Chain[];
    magicSdkConfiguration: MagicSDKAdditionalConfiguration | undefined;
    connectedChainId: number | undefined;
    constructor(options: MagicConnectorOptions);
    connect(options: MagicConnectorOptions): Promise<string>;
    disconnect(): Promise<void>;
    getAddress(): Promise<string>;
    getSigner(): Promise<Signer>;
    getProvider(): Promise<providers.Provider>;
    switchChain(chainId: number): Promise<void>;
    isConnected(): Promise<boolean>;
    setupListeners(): Promise<void>;
    updateChains(chains: Chain[]): void;
    onDisconnect(): void;
    getMagicSDK(): Magic;
    initializeMagicSDK({ chainId }?: {
        chainId?: number;
    }): Magic;
    getChainId(): Promise<number>;
    protected isChainUnsupported(chainId: number): boolean;
    protected onChainChanged(chainId: string | number): void;
}
