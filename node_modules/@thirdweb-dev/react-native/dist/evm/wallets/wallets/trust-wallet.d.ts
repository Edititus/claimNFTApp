import { WalletOptions } from "@thirdweb-dev/react-core";
import { WCMeta } from "../types/wc";
import { WalletConnectBase } from "./wallet-connect/WalletConnectBase";
import { WalletConnectConfig } from "./wallet-connect/wallet-connect";
export declare class TrustWallet extends WalletConnectBase {
    static id: "trust";
    static meta: {
        name: string;
        iconURL: string;
        links: {
            native: string;
            universal: string;
        };
    };
    getMeta(): WCMeta;
}
export declare const trustWallet: (config?: WalletConnectConfig) => {
    id: "trust";
    meta: {
        name: string;
        iconURL: string;
        links: {
            native: string;
            universal: string;
        };
    };
    create: (options: WalletOptions) => TrustWallet;
    recommended: boolean | undefined;
};
