import { LocalWallet as LocalWalletCore, Connector } from "@thirdweb-dev/wallets";
export declare class LocalWallet extends LocalWalletCore {
    static meta: {
        name: string;
        iconURL: string;
    };
    getMeta(): {
        name: string;
        iconURL: string;
    };
    generate(): Promise<string>;
    protected getConnector(): Promise<Connector>;
}
