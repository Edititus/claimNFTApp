import { LocalWallet } from "./LocalWallet";
import { WalletOptions } from "@thirdweb-dev/wallets";
export declare const localWallet: () => {
    id: string;
    meta: {
        name: string;
        iconURL: string;
    };
    create: (options: WalletOptions) => LocalWallet;
};
