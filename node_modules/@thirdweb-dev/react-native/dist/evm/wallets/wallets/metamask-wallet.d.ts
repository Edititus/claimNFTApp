import { WalletOptions } from "@thirdweb-dev/react-core";
import { WCMeta } from "../types/wc";
import { WalletConnectBase } from "./wallet-connect/WalletConnectBase";
import { WalletConnectConfig } from "./wallet-connect/wallet-connect";
export declare class MetaMaskWallet extends WalletConnectBase {
    static id: "metamask";
    static meta: {
        name: string;
        iconURL: string;
        links: {
            native: string;
            universal: string;
        };
    };
    getMeta(): WCMeta;
}
export declare const metamaskWallet: (config?: WalletConnectConfig) => {
    id: "metamask";
    meta: {
        name: string;
        iconURL: string;
        links: {
            native: string;
            universal: string;
        };
    };
    create: (options: WalletOptions) => MetaMaskWallet;
    recommended: boolean | undefined;
};
