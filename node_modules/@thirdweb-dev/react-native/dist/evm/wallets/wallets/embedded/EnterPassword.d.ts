import React from "react";
export type EnterPasswordProps = {
    goBack: () => void;
    close: () => void;
    email: string;
    type: "create_password" | "enter_password";
    onPassword: (password: string) => void;
    error: string;
};
export declare const EnterPassword: ({ close, goBack, email, type, onPassword, error, }: EnterPasswordProps) => React.JSX.Element;
