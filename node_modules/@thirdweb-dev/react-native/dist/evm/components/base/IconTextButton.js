import BaseButton from "./BaseButton";
import Text from "./Text";
import { StyleSheet } from "react-native";
export const IconTextButton = ({ icon, onPress, text, p, ...args }) => {
    return (<BaseButton p={p || "sm"} backgroundColor="background" borderRadius="md" borderWidth={0.5} flexDirection="row" justifyContent="flex-start" alignItems="center" onPress={onPress} borderColor="border" {...args}>
      {icon}
      <Text variant="bodySmall" style={styles.networkText}>
        {text}
      </Text>
      {/* <RightArrowIcon size={10} /> */}
    </BaseButton>);
};
const styles = StyleSheet.create({
    networkText: {
        marginLeft: 8,
    },
});
