import { ReactNode } from "react";
import { Theme } from "../../styles/theme";
import BaseButton from "./BaseButton";
type IconTextProps = {
    icon?: ReactNode;
    text: string;
    padding?: keyof Theme["spacing"];
    onPress: () => void;
} & React.ComponentProps<typeof BaseButton>;
export declare const IconTextButton: ({ icon, onPress, text, p, ...args }: IconTextProps) => import("react").JSX.Element;
export {};
