/// <reference types="react" />
import BaseButton from "./BaseButton";
import { Palette } from "../../styles/colors";
type WalletButtonProps = {
    onPress?: () => void;
    walletIconUrl: string;
    name: string;
    nameColor?: keyof Palette;
    labelText?: string;
    recommended?: boolean;
    iconWidth?: number;
    iconHeight?: number;
} & React.ComponentProps<typeof BaseButton>;
export declare const WalletButton: ({ iconWidth, iconHeight, mb, onPress, walletIconUrl, name, nameColor, labelText, recommended, ...props }: WalletButtonProps) => import("react").JSX.Element;
export {};
