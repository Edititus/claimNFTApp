import { ReactElement } from "react";
import { Theme } from "../../styles/theme";
export declare const TOAST_HEIGHT = 45;
export type ToastProps = {
    text: string;
    icon?: ReactElement;
    backgroundColor?: keyof Theme["colors"];
    translateY?: number;
};
export declare function Toast({ text, icon, backgroundColor, translateY, }: ToastProps): import("react").JSX.Element;
