import { TouchableOpacity } from "react-native";
import Text from "./Text";
export const Link = ({ text, variant = "link", onPress, ...props }) => {
    return (<TouchableOpacity onPress={onPress}>
      <Text variant={variant} {...props}>
        {text}
      </Text>
    </TouchableOpacity>);
};
