import { ModalHeaderTextClose } from "../base/modal/ModalHeaderTextClose";
export const NetworkSelectorModal = ({ onClose, }) => {
    return (<ModalHeaderTextClose headerText={"Select Network"} subHeaderText={"Choose a new network to connect to thirdweb"} onClose={onClose}/>);
};
