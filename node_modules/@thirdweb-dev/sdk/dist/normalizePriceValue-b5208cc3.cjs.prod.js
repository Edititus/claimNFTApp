'use strict';

var index = require('./index-6091771f.cjs.prod.js');
var ethers = require('ethers');
var fetchCurrencyValue = require('./fetchCurrencyValue-c78807a7.cjs.prod.js');

/**
 *
 * @param provider
 * @param inputPrice
 * @param currencyAddress
 * @returns
 * @internal
 */
async function normalizePriceValue(provider, inputPrice, currencyAddress) {
  const metadata = await fetchCurrencyValue.fetchCurrencyMetadata(provider, currencyAddress);
  return ethers.utils.parseUnits(index.AmountSchema.parse(inputPrice), metadata.decimals);
}

exports.normalizePriceValue = normalizePriceValue;
